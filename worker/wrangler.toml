name = "investment-alpha-bot"
main = "index.js"
compatibility_date = "2026-01-01"

# Secrets are NOT stored here. After `npx wrangler login`, set them with:
#   npx wrangler secret put DISCORD_PUBLIC_KEY
#   npx wrangler secret put OWNER_ID
#   npx wrangler secret put GH_TOKEN
#   npx wrangler secret put GH_REPO        (value: srijanbansaljob-a11y/investment-alpha)
# Deploy with:
#   npx wrangler deploy
